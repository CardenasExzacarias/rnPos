import React from 'react'
import { Text, View } from 'react-native'

const SaleScreen = () => {
    return (
        <View>
            <Text>Sales</Text>
        </View>
    );
};

export default SaleScreen;